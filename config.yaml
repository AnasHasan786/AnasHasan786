# config.yaml
# ------------------------------------------------------------------
# Edit this file to retarget the dashboard — no Python changes needed.
# The workflow's GITHUB_USERNAME env var (in dashboard.yml) still wins
# if both are set, so you can also override per-run via Actions.
# ------------------------------------------------------------------

username: AnasHasan786

# How many weeks of history to show in the Weekly Activity / Commit Trend charts.
weekly_activity_weeks: 12

# How many months of history to show in the Repository Growth chart.
repo_growth_months: 12

# Max number of distinct languages shown before grouping the remainder as "Other".
language_top_n: 6
